import 'dart:ui';


// Todo: figure out overall theme constants for app e.g padding, space, fontWeights, etc

// Make a proper way of accessing these colors (primary // accent // etc)

const blueColor = Color.fromRGBO(34, 73, 179, 1);
const pinkColor = Color.fromRGBO(255, 115, 115, 1);
const borderColor = Color.fromRGBO(41, 52, 73, 1);
const backgroundColor = Color(0xffB0C1E1);

// For gradient

const gradGreen = Color.fromRGBO(152, 240, 213, 1);
const gradYellow = Color.fromRGBO(253, 209, 123, 1);
